server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://34.47.74.27:3306/smartWatcha
    username: smartwatcha
    password: 123qwe!@#
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  internal-token: 123qwe!@#
  email:
    verification:
      expire-minutes: 1
      cooldown-seconds: 60 